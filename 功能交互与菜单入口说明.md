# 增重/减重双模式（befat）功能交互与入口路径说明

> 本文档梳理小程序全部页面的功能交互、菜单进入路径与入口路径，供开发与评审参考。页面注册入口见 `miniprogram/app.json`。

## 〇、双目标模式（核心）

小程序支持**增重（gain）**与**减重（lose）**双目标模式，建档时由用户选择，`goal_type` 字段贯穿全链路：

| 维度 | 增重 (gain) | 减重 (lose) |
|---|---|---|
| 每日食谱入口 | 今日增肥食谱（4 餐） | 今日燃脂锦囊（3 条生活建议） |
| 统计入口 | 达标统计 | 热量与缺口 |
| 附近推荐入口 | 🍲 约饭吧 | 🏃 去运动 |
| 周边检索 | 餐饮美食 POI | 运动场所 POI |
| 达标判定 | 热量/蛋白 ≥ 目标 | 热量 ≤ 上限 且 蛋白 ≥ 保底 |
| BMI 标签 | 偏瘦/正常/超重 | 偏瘦/正常/超重 |

所有 `goal_type` 读取统一 `normalizeGoalType(v) => v === 'lose' ? 'lose' : 'gain'` 兜底，老用户缺失字段默认增重。

## 一、应用入口与启动逻辑

微信小程序，云开发环境 `cloud1-d4ghksio8431633f8`，应用标题「增重记录」。注册页面共 9 个：

| 页面路径 | 作用 |
|---|---|
| `pages/onboarding/onboarding` | 引导 / 新用户建档（设置目标 + 目标方向） |
| `pages/index/index` | **首页（主枢纽）** |
| `pages/log-food/log-food` | 记录一餐（文本 / 拍照解析食物） |
| `pages/weight-track/weight-track` | 称体重 + 体重曲线 |
| `pages/daily-menu/daily-menu` | 今日食谱（gain）/ 燃脂锦囊（lose）+ 附近推荐 + 收藏 |
| `pages/stats/stats` | 达标统计（gain）/ 热量与缺口（lose） |
| `pages/goal-detail/goal-detail` | 目标详情（进度趋势）+ 称体重/修改目标入口 |
| `pages/target-edit/target-edit` | 修改目标 |
| `pages/profile/profile` | 我的档案 / 设置 |

**启动判断**（`app.js:34` + `onboarding.js:21`）：启动后查询 `users` 集合，若已建档（`target_weight_kg != null`）则放行进首页；否则进入 `onboarding` 填写档案，提交后 `reLaunch` 回首页。

> 注：目录下另有 `my-favorites / recipe-detail / recipe-list` 三个页面，但未在 `app.json` 注册、无任何跳转引用，为遗留废弃页。

## 二、首页（index）—— 所有功能的入口枢纽

首页布局从上到下（`index.wxml`）：

1. **顶部 hero**：👤「我的」按钮 → `profile`；问候语按时间段 + 目标方向双维度变化；达成目标时显示 🎯 徽章、未达成显示「冲刺中」
2. **双环进度**：热量 / 蛋白质 当前值 vs 目标（canvas 圆环）；减重模式热量超标时切换警示红（#FF4D4F），并显示「还可吃 N kcal / 已超标 N kcal」
3. **快捷操作区（4 个）**：
   - 🍽️ 记录一餐 → `log-food`
   - 🍲 约饭吧（gain）/ 🏃 去运动（lose）→ `daily-menu?view=poi`（直达附近推荐）
   - 📖 今日增肥食谱（gain）/ 今日燃脂锦囊（lose）→ `daily-menu`
   - 📈 达标统计（gain）/ 热量与缺口（lose）→ `stats`
4. **目标进度卡片**：
   - 顶部标题 + 右箭头；点击整卡 → `goal-detail`
   - 「还需增重/减重 X kg」行右侧内嵌 BMI 状态标签（`BMI 20.3 (正常)`，按偏瘦/正常/超重动态配色）
   - 进度条 + 底部三点「初始 / 当前 / 目标」
   - 无目标时显示引导卡 → `onboarding`
5. **今日已吃清单**：分餐展示（早餐 / 午餐 / 晚餐 / 加餐），无记录时显示空态引导

> 注：「修改目标」与「⚖️ 称体重」入口已**迁移**至目标详情页（`goal-detail`）卡片内，首页不再直接提供。称体重功能仍存在于 `weight-track` 页面（可通过目标详情页进入）。

跳转入口集中在 `index.js` 的 `goToXxx` 方法，均用 `wx.navigateTo`（onboarding 提交用 `reLaunch`）。

## 三、各页面功能交互与进入路径

### 1. 目标引导 / 建档 `onboarding`
- **入口**：首次启动自动进入；首页无目标引导卡「去设定」→ 进入
- **交互**：三步表单
  - 第 1 步：性别 + 年龄（10-100）
  - 第 2 步：**目标方向（增重/减重，默认增重）** + 身高、当前体重、目标体重、计划周期（1-104 周）；方向决定目标体重需大于/小于当前体重
  - 第 3 步：活动水平（久坐不动/轻度/中度/高度/极高强度）
- **提交**：调用云函数 `calcTarget`（按方向计算每日热量/蛋白目标）→ 写库 → `reLaunch` 回首页并标记强制刷新

### 2. 记录一餐 `log-food`
- **入口**：首页「🍽️ 记录一餐」
- **交互**：
  - 选餐次（早餐 / 午餐 / 晚餐 / 加餐）
  - 输入文字描述，或点相机图标拍照（`wx.chooseMedia` → 压缩到 800px → base64）
  - 点「估算热量」→ 云函数 `parseFoodLog`（支持文本与图片两种）
  - 展示解析结果，可编辑每项名称 / 份量 / 热量 / 蛋白质，可删除、新增菜品
  - 「✅ 记下来」→ `saveFoodLog` → 庆祝动画 → `navigateBack` 回首页

### 3. 称体重 `weight-track`（无显式入口，待清理）
- **现状**：首页「⚖️ 称体重」已废弃（改为「约饭吧/去运动」）；目标详情页改用内联弹窗称体重（`onRecordWeight` → 数字输入 → `saveWeightLog` → 刷新目标进度与趋势图）。
- `weight-track` 页面仍注册在 `app.json` 但已无跳转入口，属待清理页。

### 4. 今日食谱 / 燃脂锦囊 `daily-menu`（双模式分流）
- **入口**：首页「📖 今日增肥食谱 / 今日燃脂锦囊」；或首页「👍 约饭吧 / 去运动」直达附近推荐（`?view=poi`）
- **增重模式**：
  - 进入自动 `getDailyMenu` 生成当日 4 餐食谱（首次加载有骨架屏）
  - 「🔄 换一换」→ 重新生成（有次数限制，code 94 提示已用完）
  - 每道菜可点「查看食材与步骤」→ 懒加载 `getMealDetail`
  - 每道菜 ❤️ 收藏 → `toggleFavoriteRecipe`
  - 「🙋 我的菜库」→ 底部半屏抽屉，按「全部 / 早餐 / 正餐 / 加餐」筛选收藏（`getFavorites`），可展开详情 / 取消收藏
- **减重模式**：
  - 进入自动 `getDailyMenu` 生成 3 条生活减脂建议（tips 清单，无收藏/热量交互）
  - 「🔄 换一换」→ 重新生成锦囊
- **附近推荐视图（`currentView === 'poi'`）**：
  - 顶部手绘复古搜索卡（搜索框 / 💡提示语 / 快捷标签 / 「帮我找」通栏按钮）
  - AI 推荐 Banner（✨ 逐字打字机展示推荐理由 `reason`）
  - POI 卡片列表：标题 / 类目 / 距离 / 人均·评分标签 / 地址 + 导航（`wx.openLocation`）/ 电话（`wx.makePhoneCall`）/ 加载更多
  - 底层走 `utils/map.js → 云函数 getNearbyPoi`（多服务商适配器，见双模式引擎说明书 §4.2）

### 5. 达标统计 / 热量与缺口 `stats`
- **入口**：首页「📈 达标统计 / 热量与缺口」
- **交互**：`getStats` 取近 90 天数据
  - gain：近 7/30 天热量达标率、蛋白达标率
  - lose：热量控制成功率、肌肉保护伞（蛋白保底）；周柱状图在控制成功率 < 100%（有超标日）时警示红
  - 体重曲线（数据点按达标/超标着色）

### 6. 目标详情 `goal-detail`
- **入口**：首页「目标进度」卡片整卡点击
- **交互**：`getGoalProgress` 展示当前/目标体重（2 位小数）、进度%、预计达成日期、体重趋势图（含目标虚线）；卡片内双按钮：
  - 「⚖️ 称体重」：弹窗输入今日体重 → `saveWeightLog` → 刷新页面
  - 「修改目标」→ `target-edit`

### 7. 修改目标 `target-edit`
- **入口**：目标详情页「修改目标」按钮
- **交互**：顶部显示当前模式徽章（增重模式/减重模式），双模式切换
  - **重新计算**：改当前体重 / 目标体重 / 计划周期 → `recalcTarget` 重算每日目标（同步更新当前体重到 weight_logs）；前端按方向校验「目标须大于/小于当前体重」
  - **手动微调**：直接填每日热量 / 蛋白 / 目标体重 → `updateTargetManual`
  - 提交后 `navigateBack` 并刷新首页

### 8. 我的 `profile`（已精简）
- **入口**：首页顶部「👤 我的」
- **交互**（`profile.wxml`）：
  - 档案卡：身高 / 当前体重 / 目标体重 + BMI 范围条（偏瘦 / 正常 / 偏高）
  - 每日目标展示（热量 / 蛋白质 / 活动水平）
  - 「重置为新用户」→ `resetUserData`（保留账号清数据）→ `reLaunch` onboarding
  - 「健康提示」→ 常驻展开的黄色警告卡片（无折叠交互）

> 注：原「重新设置目标」「吃饭提醒」「导出数据」「删除所有数据」入口已移除。

## 四、页面跳转关系图（入口路径）

```
启动
 └─ app.checkUserStatus → 已建档 ? → 首页 index
                       └─ 未建档 ──→ onboarding ──(提交成功 reLaunch)──→ index

首页 index（枢纽）
 ├─ 记录一餐 ──→ log-food ──(保存后 navigateBack)──→ index
 ├─ 约饭吧/去运动 ──→ daily-menu?view=poi（附近推荐）
 ├─ 今日增肥食谱/燃脂锦囊 ──→ daily-menu（含 收藏抽屉）
 ├─ 达标统计/热量与缺口 ──→ stats
 ├─ 目标进度卡 ──→ goal-detail ──(⚖️ 称体重/修改目标)──→ 弹窗称重 或 target-edit
 ├─ 无目标引导卡 ──→ onboarding
 └─ 👤 我的 ──→ profile
                └─ 重置为新用户 ──(reLaunch)──→ onboarding
```

补充说明：所有写操作（记录饮食 / 称体重 / 改目标 / 建档 / 重置）完成后，都会通过 `app.globalData.forceIndexRefresh` 或来源页路由判断（`DIRTY_SOURCE_ROUTES`，`index.js`）触发首页强制刷新；其余情况走 30s TTL 缓存以节省云函数配额。