# 《"拍照识菜"功能增量升级详细设计规格书》

> 项目：BeFat 增肥记录小程序
> 分支：`图像识别菜品`
> 基线版本：`refactor/recipe @ 2ad9682`
> 涉及范围：`miniprogram/pages/log-food/*`、`cloudfunctions/parseFoodLog/*`
> 状态：已实现（图片模式已重构为「单次多模态调用」）

---

## 0. 文档约定与术语

| 术语 | 说明 |
|------|------|
| 图片模式（视觉） | 智谱视觉模型（默认 `glm-4v-flash`，可经 `VISION_MODEL` 切换），**看图后直接输出营养 JSON**（份量预估 + 热量 + 蛋白质） |
| 文本模式（营养） | 文本模型（默认 `deepseek-v4-flash`，可经 `NUTRITION_MODEL` 切换），根据文字描述输出营养 JSON |
| Base64 | 前端离屏 Canvas 压缩后导出的 JPEG 纯 base64 字符串（**不含** `data:image/jpeg;base64,` 前缀） |
| 降级链路 | 任一模式失败时回退到"纯文字手动输入"的能力，保证主流程不因识图失败而不可用 |

---

## 1. 系统架构与交互时序图

### 1.1 总体数据流

```
用户操作                        前端 (log-food)                          云函数 parseFoodLog
─────────                      ─────────────────                        ─────────────────────
点相机图标 / 选图/拍照
      │
      ├─▶ wx.chooseMedia ─────────────────────────────────▶ (系统相册/相机)
      │
      ◀── tempFilePath
      │
      ├─▶ wx.getImageInfo (读宽高)
      │
      ├─▶ 离屏 Canvas 等比压缩 (Max 800px)
      │
      ├─▶ canvas.toDataURL('image/jpeg', 0.8)
      │         │
      │         └─ 剥离 data URI 前缀 → 纯 base64
      │
      ├─▶ wx.cloud.callFunction({ name:'parseFoodLog',
      │            data:{ image_base64, meal_type, date, raw_text? } })
      │                                                 │
      │                                      ┌──────────▼───────────┐
      │                                      │ 参数校验 + 大小防御   │
      │                                      ├───────────────────────┤
      │                                      │ imgSecCheck 图片安全  │
      │                                      ├───────────────────────┤
      │                                      │ 视觉模型 单次多模态    │ (12s 超时)
      │                                      │   → 直接输出营养 JSON │
      │                                      └──────────┬───────────┘
      ◀──── result.data.items (与纯文字链路同构) ───────┘
      │
      ├─▶ 复用现有 setData → parsedItems / recalcTotal
      │
      └─▶ 用户编辑修正 → saveFoodLog（与现状完全一致）
```

### 1.2 关键设计决策

1. **图片模式单次多模态调用**：视觉模型（`glm-4v-flash`）看图后**直接输出营养 JSON**，一次网络往返完成"识菜 + 营养计算"，省去原"第一棒识菜 → 第二棒算营养"的串行二次调用，降低延迟。

2. **出参与纯文字链路 100% 同构**：云函数最终返回的 `data.items / data.total_calorie / data.total_protein_g` 与现有 `parseFood` 结果结构完全一致。前端 `parseFood()` 只需新增一个"图片模式"分支，结果处理（`setData`、编辑、保存）零改动。

3. **Base64 纯串传输**：云函数侧需要把 base64 转 `Buffer` 时自行前缀 `data:image/jpeg;base64,`，避免前端重复吞前缀、后端重复剥前缀的耦合。

### 1.3 时序图（Sequence Diagram）

```
┌────────┐  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ 用户    │  │ log-food│  │ 云函数    │  │ imgSec   │  │   GLM    │
└───┬────┘  └────┬────┘  │ parseFood│  │ Check    │  │ 视觉模型  │
    │            │       └────┬─────┘  └────┬─────┘  └────┬─────┘
    │ 点击相机    │            │             │             │
    ├───────────▶│            │             │             │
    │            │ chooseMedia│             │             │
    │            ├─压缩→base64│             │             │
    │            │            │             │             │
    │            │ callFunction(image_base64)             │
    │            ├───────────▶│             │             │
    │            │            ├─ 大小防御 ──┤             │
    │            │            ├──imgSecCheck─────────────▶│
    │            │            │◀──────────── 通过/违规 ────┤
    │            │            │ 单次多模态 image+营养prompt │
    │            │            ├────────────────────────────▶│
    │            │            │◀─────── 直接返回营养 JSON ────┤
    │            │            │             │             │
    │            │◀── result.data.items ────┤             │
    │◀─ 编辑/保存 ─┤            │             │             │
```

---

## 2. 前端模块详细设计

### 2.1 WXML 节点结构

在现有 `textarea` 外层新增一个相对定位包裹容器 `input-wrap`，相机图标作为其右侧绝对定位子节点，**不改变既有文本解析流程**。

```xml
<!-- 现有第 12-16 行 card 内改造 -->
<view class="card">
  <text class="label">说说你今天吃了啥 🍽️</text>

  <view class="input-wrap">
    <textarea class="food-input" placeholder="例：两个大鸡腿➕一碗米饭➕一杯全脂牛奶"
              value="{{rawText}}" bindinput="onTextInput" maxlength="500" auto-height />
    <view class="camera-trigger" bindtap="chooseMealImage">
      <image class="camera-icon" src="/assets/icons/camera.svg" mode="aspectFit" />
    </view>
  </view>

  <view class="input-hint">💡 描述得越详细，估算结果越准确 · 也可拍照识菜</view>
</view>
```

说明：
- `.camera-trigger` 使用 `bindtap`（不是 `catchtap`），点击时不会中断 `textarea` 的聚焦/失焦行为。
- 图标资源建议放 `miniprogram/assets/icons/camera.svg`（极简线性相机图标，与现有 emoji/图标风格一致）；若无 SVG 支持顾虑，可用 PNG `@2x/@3x`。
- 可选增强：`rawText` 为空时图标高亮引导，非空时弱化（通过 class 条件渲染）。

### 2.2 WXSS 定位样式

沿用现有 CSS 变量约定（`--primary`、`--border`、`--text-light`、`--danger`），新增以下样式：

```css
.input-wrap {
  position: relative;
  width: 100%;
}

/* textarea 右侧留出图标安全区，避免文字顶到图标 */
.input-wrap .food-input {
  padding-right: 90rpx;
}

.camera-trigger {
  position: absolute;
  right: 16rpx;
  bottom: 14rpx;
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #FFF5EB;
  border: 2rpx solid var(--border);
  border-radius: 50%;
}

.camera-trigger:active {
  background: #FFE6CC;
}

.camera-icon {
  width: 32rpx;
  height: 32rpx;
}

/* 识别中 loading 态 */
.camera-trigger.loading {
  opacity: 0.5;
  pointer-events: none;
}
```

注意：`textarea` 原生组件层级较高，需确认 `input-wrap` 包裹下相机按钮可正常点击（小程序中 textarea 为原生组件，绝对定位的覆盖卡片需用 `cover-view`/`cover-image` 才能覆盖原生组件）。**兜底方案**：若真机出现相机按钮被 textarea 遮挡无法点击，改用 `<cover-view class="camera-trigger">` + `<cover-image>` 替代 `view/image`，样式不变。

### 2.3 Canvas 压缩算法（离屏）

data 新增字段：`imageLoading: false`。

JS 核心逻辑（新增方法，不触碰现有 `parseFood` 文本链路）：

```js
// 相机图标回调
chooseMealImage() {
  if (this.data.parsing || this.data.imageLoading) return
  wx.chooseMedia({
    count: 1,
    mediaType: ['image'],
    sourceType: ['album', 'camera'],   // 相册 + 相机都支持
    sizeType: ['compressed'],
    success: async (res) => {
      const tempPath = res.tempFiles[0].tempFilePath
      try {
        this.setData({ imageLoading: true })
        const base64 = await this.compressImage(tempPath)
        await this.parseFoodByImage(base64)
      } catch (err) {
        logger.error('chooseMealImage', err)
        wx.showToast({ title: '图片处理失败', icon: 'none' })
      } finally {
        this.setData({ imageLoading: false })
      }
    }
  })
},

// 离屏 Canvas 等比压缩，Max 800px，导出 jpeg base64（无 data URI 前缀）
compressImage(src) {
  return new Promise((resolve, reject) => {
    wx.getImageInfo({
      src,
      success: (info) => {
        const MAX = 800
        const ratio = Math.min(1, MAX / Math.max(info.width, info.height))
        const w = Math.round(info.width * ratio)
        const h = Math.round(info.height * ratio)

        const canvas = wx.createOffscreenCanvas({ type: '2d', width: w, height: h })
        const ctx = canvas.getContext('2d')
        const img = canvas.createImage()
        img.onload = () => {
          ctx.drawImage(img, 0, 0, w, h)
          // jpeg 质量 0.8；png 含透明时改用 image/png
          const dataUrl = canvas.toDataURL('image/jpeg', 0.8)
          const base64 = dataUrl.split(',')[1] || ''
          if (!base64) return reject(new Error('canvas toDataURL empty'))
          resolve(base64)
        }
        img.onerror = () => reject(new Error('image load failed'))
        img.src = src
      },
      fail: reject
    })
  })
},

// 图片模式调云函数（出参结构与纯文字一致，复用结果处理）
async parseFoodByImage(base64) {
  this.setData({ parsing: true })
  try {
    const today = util.formatDate(new Date())
    const res = await wx.cloud.callFunction({
      name: 'parseFoodLog',
      data: {
        image_base64: base64,
        meal_type: this.data.mealType,
        date: today
      }
    })
    this.handleParseResult(res.result)   // 抽取公共结果处理（见 2.4）
  } catch (err) {
    logger.error('parseFoodByImage', err)
    wx.showToast({ title: '网络异常，请重试', icon: 'none' })
    this.setData({ parsing: false })
  }
}
```

### 2.4 Base64 导出与结果处理复用

当前 `parseFood()` 内 `result.code !== 0` 之后的结果处理（`setData showResult/parsedItems/totalCalorie/totalProtein/rawTextSaved`）应抽取为公共方法 `handleParseResult(result)`，供文本链路与图片链路共用，避免重复。图片模式的 `rawTextSaved` 由云函数返回的 `raw_text` 字段回填（见第 4 节出参）。

压缩质量策略（防御性）：
- 压缩后 JPEG base64 若仍 > 2.5MB（对应原始约 1.9MB），按 0.8 → 0.6 递减重压，直至通过或失败。
- 前端提前拦截过大图片，避免云函数侧二次校验返回失败造成一次无效往返。

---

## 3. 云函数后端详细设计（parseFoodLog）

### 3.1 按模式单次调用逻辑

图片模式与文本模式互斥，各自**单次调用对应模型**，不再有串行第二棒：

```js
exports.main = async (event, context) => {
  const { raw_text, image_base64, meal_type, date } = event
  const isImageMode = !!image_base64
  // ① 基础校验：meal_type/date 必填；raw_text 与 image_base64 至少其一

  if (isImageMode) {
    // ② 大小防御（见 5.1）→ code 90
    // ③ 图片安全检测 imgSecCheck（见 5.3）→ code 91 / 89
    // ④ 单次多模态：视觉模型直接输出营养 JSON（12s 超时）
    const parsed = await runVisionNutrition(image_base64)   // 失败 / JSON 解析失败 → code 92
    return { code: 0, data: { raw_text: <items 拼凑>, meal_type, date, ...parsed } }
  }

  // 文本模式：msgSecCheck（→ code 88 / 89）→ 文本营养模型单次调用（15s 超时）
  const parsed = await runDeepSeekNutrition(raw_text)        // 失败 → code 3
  return { code: 0, data: { raw_text, meal_type, date, ...parsed } }
}
```

### 3.2 视觉 Prompt 与请求（单次多模态）

视觉 prompt 要求看图后**直接输出营养 JSON**（份量 + 热量 + 蛋白质），拿到 content 后经 `parseNutritionJson` 清洗 Markdown 并解析：

```js
const VISION_NUTRITION_PROMPT = '你是一个中国食物营养分析专家。请看图片中的食物，直接估算每种食物的名称、份量、热量(kcal)和蛋白质(g)。要求：1. 份量用中文描述；2. 热量和蛋白质依据中国常见食物营养成分数据估算；3. 图片不清晰时按常见份量估算。必须只返回严格的JSON，不要任何解释文字、不要Markdown代码块：{"items":[{"name":"食物名","portion":"份量描述","calorie":数值,"protein_g":数值}],"total_calorie":数值,"total_protein_g":数值}'

function parseNutritionJson(content) {
  const result = JSON.parse(stripCodeFence(content))   // JSON.parse 失败向上抛 → code 92
  if (!result.items || !Array.isArray(result.items)) throw new Error('Unexpected response format')
  const parsedItems = result.items.map(item => ({
    name: item.name || '未知食物',
    portion: item.portion || '1份',
    calorie: Math.round(Number(item.calorie) || 0),
    protein_g: Math.round((Number(item.protein_g) || 0) * 10) / 10
  }))
  const totalCalorie = result.total_calorie || parsedItems.reduce((s, i) => s + i.calorie, 0)
  const totalProteinG = result.total_protein_g || parsedItems.reduce((s, i) => s + i.protein_g, 0)
  return { items: parsedItems, total_calorie: totalCalorie, total_protein_g: totalProteinG }
}

async function runVisionNutrition(imageBase64) {
  const apiKey = process.env.VISION_API_KEY || process.env.ZHIPU_API_KEY
  if (!apiKey) throw new Error('VISION_API_KEY not configured')
  const visionModel = process.env.VISION_MODEL || process.env.ZHIPU_VISION_MODEL || 'glm-4v-flash'
  const apiUrl = process.env.VISION_API_URL || VISION_API_URL_DEFAULT

  const resp = await axios.post(apiUrl, {
    model: visionModel,
    messages: [{
      role: 'user',
      content: [
        { type: 'text', text: VISION_NUTRITION_PROMPT },
        { type: 'image_url', image_url: { url: 'data:image/jpeg;base64,' + imageBase64 } }
      ]
    }],
    temperature: 0.1,
    max_tokens: 1024
  }, {
    headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + apiKey },
    timeout: 12000   // 图片模式 12s
  })

  const content = resp.data && resp.data.choices && resp.data.choices[0] && resp.data.choices[0].message && resp.data.choices[0].message.content
  if (!content) throw new Error('Vision empty response')
  return parseNutritionJson(content)
}
```

文本模式 `runDeepSeekNutrition` 结构同上，仅换 `NUTRITION_*` 环境变量、纯文本 prompt、`timeout: 15000`。

### 3.3 超时控制（按模式单次调用）

| 模式 | 模型 | 超时 | 设计依据 |
|------|------|------|----------|
| 图片 | `glm-4v-flash`（经 `VISION_MODEL` 可切） | **12s** | 视觉多模态单次，正常 3~8s；12s 留足网络抖动余量 |
| 文本 | `deepseek-v4-flash`（经 `NUTRITION_MODEL` 可切） | **15s** | 纯文本 + JSON 结构化，正常 2~5s；15s 兜底偶发慢 |

**云函数执行超时配置对齐**：
- 单次调用理论峰值 ≤ 15s，叠加 `imgSecCheck`（约 1~2s）与网络开销。
- **云函数"执行超时"维持 ≥ 20s 即可**（控制台配置），不再需要 35s（旧两步串行 27s 已取消）。

### 3.4 降级响应策略

| 场景 | 降级动作 | 返回 |
|------|----------|------|
| 图片视觉失败/超时 | 提示手动输入文字 | `code 92, message:'图片识别失败，请改用文字描述'` |
| 图片视觉 JSON 解析失败/空响应 | 同上 | `code 92` |
| 文本营养模型失败/格式异常 | 复用现有 `code 3`（AI 解析失败，请手动输入） | `code 3` |

---

## 4. 接口契约设计

### 4.1 入参 Schema（parseFoodLog）

| 字段 | 类型 | 必填 | 模式 | 说明 |
|------|------|------|------|------|
| `raw_text` | string | 文本模式必填 | 文本 | 用户文字描述，≤500 字 |
| `image_base64` | string | 图片模式必填 | 图片 | 纯 base64（无 data URI 前缀），压缩后 ≤ 约 3MB 字符 |
| `meal_type` | string | 是 | 通用 | `breakfast/lunch/dinner/snack` |
| `date` | string | 是 | 通用 | `YYYY-MM-DD` |

约束：
- `raw_text` 与 `image_base64` 至少其一；二者同时存在时**图片模式优先**（`image_base64` 生效，忽略 `raw_text`）。
- 二者皆缺 → `code 1`。

### 4.2 出参 Schema（与文本模式完全同构）

```jsonc
{
  "code": 0,                          // 0 成功
  "message": "ok",
  "data": {
    "raw_text": "红烧肉 约200g；米饭 约1碗",  // 图片模式由 items 拼凑 name+portion（前端 rawTextSaved 用）
    "meal_type": "lunch",
    "date": "2026-08-19",
    "items": [                          // 与纯文字链路结构一致
      { "name": "红烧肉", "portion": "约200g", "calorie": 480, "protein_g": 18.5 },
      { "name": "米饭",   "portion": "约1碗",  "calorie": 232, "protein_g": 5.2 }
    ],
    "total_calorie": 712,
    "total_protein_g": 23.7
  }
}
```

### 4.3 错误码总表

| code | 含义 | 触发 | 前端动作 |
|------|------|------|----------|
| `0` | 成功 | — | 正常渲染结果 |
| `1` | 缺参数 | 无 `raw_text` 且无 `image_base64`，或缺 `meal_type/date` | toast 提示 |
| `2` | 无效餐次 | `meal_type` 非法 | toast 提示 |
| `3` | AI 解析失败 | 文本模式营养模型失败/格式异常 | toast「AI 解析失败，请手动输入」，回退文字编辑 |
| `88` | 文本违规 | 文本 `msgSecCheck` 未通过 | toast「输入包含违规内容」 |
| `89` | 安全服务不可用 | `msgSecCheck`/`imgSecCheck` 异常 | toast「内容安全检测服务暂不可用」 |
| `90` | 图片过大 | base64 超阈值 | toast「图片过大，请压缩后重试」 |
| `91` | 图片违规 | `imgSecCheck` 未通过 | toast「图片包含违规内容」 |
| `92` | 图片识别失败 | 视觉模型失败/超时/JSON 解析失败 | toast 提示手动输入文字 |
| `-1` | 服务器内部错误 | 未捕获异常 | toast「服务器内部错误」 |

设计原则：**错误码单调向前追加**（`90~92` 为图片模式新增），不与现有 `0/1/2/3/88/89/-1` 冲突，前端与后端契约可独立演进。

---

## 5. DevSecOps 与安全设计

### 5.1 Payload 大小防御（多层拦截）

| 层 | 位置 | 规则 |
|----|------|------|
| 前端 | `compressImage` | Max 800px + jpeg 0.8；若 `base64.length > 2.5MB` 字符，递减质量 0.6 重压；仍超限则前端直接报错，不发请求 |
| 云函数入口 | `parseFoodLog` | `image_base64.length > 3_145_728`（≈3MB 字符，对应约 2.36MB 原始字节）→ 立即 `code 90`，**不进入 imgSecCheck / 不调模型** |
| 云函数 | `imgSecCheck` | 转 Buffer 前再次校验 `Buffer.byteLength(base64,'base64') <= 2MB`，防止 base64 膨胀攻击 |

> 依据：微信云函数事件体与 `imgSecCheck` media 均有大小上限，2MB 原始图是安全上限；800px 压缩后正常远低于此阈值。

### 5.2 密钥隔离（图片/文本两模式对称配置）

| 环节 | 变量 | 默认/兜底 |
|------|------|----------|
| 视觉 key | `VISION_API_KEY` | 回退 `ZHIPU_API_KEY` |
| 视觉 model | `VISION_MODEL` | 回退 `ZHIPU_VISION_MODEL`，最终 `glm-4v-flash` |
| 视觉 url | `VISION_API_URL` | `https://open.bigmodel.cn/api/paas/v4/chat/completions` |
| 营养 key | `NUTRITION_API_KEY` | 回退 `DEEPSEEK_API_KEY` |
| 营养 model | `NUTRITION_MODEL` | `deepseek-v4-flash` |
| 营养 url | `NUTRITION_API_URL` | `https://api.deepseek.com/chat/completions` |

- 所有 Key 均通过云函数环境变量读取，不硬编码、不进 git、不进 CHANGELOG（post-commit 自动遮蔽）。
- 图片/文本两模式 Key 分别归属不同厂商，互不传递；`Authorization: Bearer` 仅在 `runVisionNutrition` / `runDeepSeekNutrition` 内部拼装，绝不写入日志（`logger.sanitize` 需对 `image_base64` 做脱敏，避免把大段 base64 写进日志）。
- 换厂商只需改对应 `*_API_URL` + `*_MODEL` + `*_API_KEY` 三项环境变量，无需改代码、重新部署。

### 5.3 图片内容安全（补齐现状空白）

现有代码仅对**文本**做了 `msgSecCheck`，图片链路**必须**走微信图片审核 `cloud.openapi.security.imgSecCheck`：

```js
const buf = Buffer.from(image_base64, 'base64')
const sec = await cloud.openapi.security.imgSecCheck({
  media: { contentType: 'image/jpeg', value: buf }
})
```

- 返回 `errCode !== 0` → `code 91`。
- 异常（权限/服务不可用）→ 与文本模式 `code 89` 语义一致，但为图片新增 `code 91` 或复用 `89`（建议**复用 89** 并在 message 区分，减少新错误码）；本文档采用独立 `91` 明确语义。

### 5.4 异常兜底与可观测性

| 场景 | 兜底 |
|------|------|
| 任一模型超时（`ECONNABORTED`） | 捕获后记录 `logger.warn`，走对应降级错误码（图片 `code 92` / 文本 `code 3`） |
| 视觉模型返回非 JSON / 空内容 | `parseNutritionJson` 内 `stripCodeFence` 剥壳 + `JSON.parse` try-catch，失败抛错 → `code 92` |
| 文本营养模型返回非 JSON | 复用 `parseNutritionJson`（同样的剥壳 + 解析 + try-catch）→ `code 3` |
| `imgSecCheck` 无权限 | 走 catch，复用 `code 89` 语义（安全服务不可用），**不阻断**图片链路但记录告警（因真机/工具环境差异，见 README 已知排坑 9.5） |

日志纪律：
- 入口 `logger.info(FN,'invoke', { mode:'image', meal_type, date, image_size: image_base64?.length })`，**记录 base64 长度而非内容**。
- 关键节点埋点：各模式模型调用耗时、imgSecCheck 耗时，输出 `duration` 字段，便于后续定位慢查询与超时瓶颈。

### 5.5 灰度与回滚

- 图片识别为**增量能力**，纯文字链路（现状）零改动、零回归。
- 前端相机图标默认隐藏（`feature flag` 本地开关）或直接上线，若 GLM 服务异常，云端降级 `code 92` 已保证用户可回退手动输入。
- 因不涉及数据库 schema 变更、无新增集合，`DEPLOY: cloudfunctions/parseFoodLog`，`DATA IMPACT: 无`。

---

## 6. 交付清单与部署影响

| 项 | 内容 |
|----|------|
| 前端改动 | `log-food.wxml`（input-wrap + 相机触发点）、`log-food.wxss`（定位样式）、`log-food.js`（chooseMealImage / compressImage / parseFoodByImage / handleParseResult 抽取）、新增 `assets/icons/camera.svg` |
| 云函数改动 | `parseFoodLog/index.js`（按模式单次调用 + `runVisionNutrition` 单次多模态 + `runDeepSeekNutrition` 文本 + `parseNutritionJson` 公共解析 + imgSecCheck + 错误码 90~92） |
| 环境变量 | `parseFoodLog` 图片/文本对称配置：视觉 `VISION_API_KEY`/`VISION_MODEL`/`VISION_API_URL` + 营养 `NUTRITION_API_KEY`/`NUTRITION_MODEL`/`NUTRITION_API_URL` |
| 云函数执行超时 | ≥ 20s（单次调用峰值 ≤ 15s，不再需要 35s） |
| 文档同步 | `README.md`（环境变量表、超时配置、注意事项）、技术方案文档（食物识别方案章节升级为"拍照识菜"已实现） |
| 数据库 | **无 schema 变更** |
| 测试 | 前端 `compressImage`（mock wx.getImageInfo/createOffscreenCanvas）、`parseFoodByImage` 降级分支；云函数 `parseFoodLog` 图片模式（mock axios/imgSecCheck，覆盖 code 90/91/92 与正常出参） |