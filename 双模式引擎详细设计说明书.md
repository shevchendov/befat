# 【增重 / 减重】双模式引擎 — 详细设计说明书与评审文档

> 版本：v1.0（对应 Phase 1/2 已落地实现 + 待规范项标注）
> 适用范围：befat 小程序全栈（`miniprogram/` + `cloudfunctions/`）

---

## 一、全局防破坏与向下兼容协议（Backward Compatibility Spec）

### 1.1 状态收敛点：统一归一化函数

全系统所有涉及 `goal_type` 的**读取**与**写入**，一律经同一收敛函数，禁止任何调用方自造三元判断或散落 `=== 'lose'` 分支：

```js
function normalizeGoalType(v) {
  return v === 'lose' ? 'lose' : 'gain'
}
```

**合法枚举仅两个**：`'gain'`（增重）、`'lose'`（减重）。任何空值、脏值、未知值一律落入 `'gain'`。

| 层级 | 落点 | 说明 |
|---|---|---|
| 云函数 | `calcTarget/recalcTarget/updateTargetManual/common/targetCalc.js` | 导出 `normalizeGoalType` 供本函数 require |
| 云函数 | `getStats` / `getGoalProgress` / `getDailySummary` / `getDailyMenu` 各自 `index.js` 顶部内联 | 这些函数不依赖 `targetCalc.js`，内联同一实现，保证语义一致 |
| 前端 | `miniprogram/utils/util.js` 导出 `normalizeGoalType` | `index.js` 通过 `util.normalizeGoalType` 复用 |
| 前端 | `daily-menu.js` 页内内联 | 页面级独立副本，逻辑与 util 完全一致 |

> 规范说明：内联副本是云函数/页面独立打包下的取舍，**语义必须逐字节一致**。评审检查点：任何新增的 `.goal_type` 判断必须走该函数。

### 1.2 数据库零侵入策略

- `users.goal_type` 为**可选新增字段**，不建集合索引、不写迁移脚本、不改既有文档。
- 读取端统一 `normalizeGoalType(user.goal_type)`，当字段为 `undefined/null/''` 时静默回退 `'gain'` —— 老用户无感升级，行为与旧版完全一致。
- `daily_menus.goal_type` 同为可选记录字段（仅用于日志/追溯），不影响读取主链路。
- **禁止**对线上库执行任何破坏性脚本（批量 update、删除、改字段类型）。

---

## 二、科学计算与目标设定详细算法（Algorithm & Formula Spec）

### 2.1 基础代谢率（BMR）

Mifflin-St Jeor 公式：

```
男性：BMR = 10 × 体重(kg) + 6.25 × 身高(cm) − 5 × 年龄 + 5
女性：BMR = 10 × 体重(kg) + 6.25 × 身高(cm) − 5 × 年龄 − 161
```

### 2.2 每日总消耗（TDEE）

```
TDEE = BMR × ActivityMultiplier
```

活动等级系数（当前实现）：

| activity_level | 系数 |
|---|---|
| sedentary | 1.2 |
| light | 1.375 |
| moderate | 1.55 |
| active | 1.725 |

> ⚠️ **规范差距**：本节设计目标为 `1.2 ~ 1.95`（含 very_active 1.95 档），当前实现最高仅 `1.725`（无 very_active 档）。评审需确认是否补档（见 §五 检查点 8）。

### 2.3 目标对比表

| 模式 | 每日热量目标公式 | 每日蛋白质目标公式 | 下限保护规则 |
|---|---|---|---|
| 增重 (gain) | TDEE + 350 kcal | 当前体重(kg) × 1.8 g | 无 |
| 减重 (lose) | max(TDEE − 500, BMR) | 当前体重(kg) × 2.0 g | 绝对不低于 BMR；周减重率控制在 0.5~1.0 kg |

> ⚠️ **规范差距（需评审对齐）**：当前实现的减重热量缺口为 **`TDEE − 350`**（与增重对称），而非设计规范的 `TDEE − 500`。二者均通过 `Math.max(round(TDEE−Δ), round(BMR))` 保证不低于 BMR。评审需决策是否统一为 500（见 §五 检查点 9）。

### 2.4 安全红线校验（validateWeights，双向）

| 校验项 | gain | lose |
|---|---|---|
| BMI < 16 | 拦截（code 2，建议先咨询医生） | 拦截（code 2，同文案） |
| 周速率 > 1 kg 增重 | 拦截（code 3） | — |
| 周速率 > 1 kg 减重 | — | 拦截（code 5） |

- 速率计算：`weeklyDelta = (targetWeight − currentWeight) / weeks`（`weeks` 缺省回退 4）。
- **增重分支 code 2/3 文案零改动**；减重新增 code 5，文案独立。
- 前端 `targetGuard.js` 同口径预校验（code 2/3/5 一致），提交前拦截，避免白填。

---

## 三、UI/UX 动态渲染与语义表达矩阵（UI & Text Specification）

### 3.1 顶部 Hero 问候语全时段映射矩阵

> 现状：`index.js` 问候语为纯增重硬编码，**尚未按 goalType 隔离**（本表为待落地规范）。

| 时间段 | 增重 (gain) 文案 | 减重 (lose) 文案 |
|---|---|---|
| < 9:00 | 早起的鸟儿有虫吃，早起的人儿要加餐！🌅 | 早起先来杯温水，早餐吃够蛋白质！🥚 |
| 9:00–12:00 | 离午饭还有一会儿，先垫垫肚子？🍙 | 上午别饿着，来点低卡高蛋白垫一垫？🥒 |
| 12:00–14:00 | 吃饱了吗？没吃饱再来一轮！🍖 | 午餐七分饱，瘦得更轻松！🥗 |
| 14:00–18:00 | 下午茶时间到，搞点零食不过分吧？🧋 | 下午加餐选错就白练了，来点轻食？🍅 |
| ≥ 18:00 | 夜宵时间！做大只的黄金时刻！🌙 | 夜宵别碰高油高糖，早睡才是燃脂王！🌙 |

> 评审检查点 4：问候语必须走 `goalType` 分支，禁止增重文案泄漏到减重用户。

### 3.2 双环与仪表盘语义表达

| 维度 | 增重 (gain) | 减重 (lose) |
|---|---|---|
| 热量语义 | 目标 ≥ X kcal（越多越达） | 上限 ≤ X kcal（控制摄入） |
| 热量副标 | 显示 `总摄入 / 目标` | 显示「还可吃 N kcal」或「已超标 N kcal」 |
| 蛋白语义 | 目标 ≥ Y g | 保底 ≥ Y g（蛋白仍需达标） |
| 进度计算 | `min(total/target, 1)` | `min(target/total, 1)`（越低越近"用满额度"） |
| 达标判定 | `total >= target` | `total <= target` |

> ⚠️ **规范差距**：当前 `index.js` 已实现 lose 进度反转与 `overLimit` 标记，但副标仍显示「当前 / 目标」，尚未实现「还可吃 / 已超标 N kcal」的差值文案（见 §五 检查点 5）。

### 3.3 超标/达标视觉反馈

- **减重模式热量超标**（`Calorie > Target`）：热量环填充色**强制切换为警示红 `#FF4D4F`**，轨道底色保持不变，外圈高亮同步红色。
- 达标态（`progress >= 1` 或减重 `total <= target`）：维持原品牌色（热量 `#FF7A2F`、蛋白 `#2ECC71`）。

> ⚠️ **规范差距**：当前实现警示红为 `#F44336`，规范权衡后建议统一为 `#FF4D4F`（更接近 Ant Design 红色体系）。评审需确认是否全局替换（见 §五 检查点 6）。

---

## 四、核心云函数逻辑与食谱隔离设计（Backend Detail Spec）

### 4.1 `getDailyMenu`（食谱生成）

**Prompt 隔离**：增重/减重各自独立常量，互不引用、互不覆盖。

| 项 | 增重 (gain) | 减重 (lose) |
|---|---|---|
| SYSTEM_PROMPT | `SYSTEM_PROMPT`（专业增重营养师），**原样保留** | `SYSTEM_PROMPT_LOSE`（专业减重营养师） |
| 每日 Prompt | `DAILY_MENU_PROMPT`，**原样保留** | `DAILY_MENU_PROMPT_LOSE` |
| 单餐热量 | 150~900（snack 80~900） | 120~600（snack 60~400），排斥高油高糖 |
| 蛋白区间 | 0~80 g | 8~60 g |

**切换逻辑**：`callGlmMenu(date, config, goalType)` 按 `goalType` 选择 `promptTemplate` 与 `systemPrompt`；`parseAndValidate(raw, goalType)` 按方向套用对应热量校验区间；main 读取 `user.goal_type` 兜底 gain。

**静态 fallback 隔离**（`config.js` 常量）：

| 常量 | 用途 |
|---|---|
| `fallback_menus`（原样，gain） | 花生酱香蕉吐司 / 鸡腿肉蛋炒饭 / 牛奶坚果燕麦杯 / 红烧牛肉面 |
| `fallback_menus_lose`（新增） | 水煮蛋燕麦牛奶粥 / 清蒸鲈鱼杂粮饭 / 黄瓜番茄鸡胸沙拉 / 虾仁豆腐蒸蛋 |

> 评审检查点 3：降级菜单必须按方向取用，`fallback_menus_lose` 缺省时回退 gain 兜底，且 gain 常量永不改动。

### 4.2 `getNearbyPoi`（周边美食）

前端动态标签映射（`daily-menu.js` `GOAL_POI_TAGS`）：

| goalType | 快捷标签 |
|---|---|
| gain | `['清淡粤菜', '地道茶楼', '海鲜大排档']` |
| lose | `['轻食沙拉', '蒸菜粥粉', '低卡减脂餐']` |

- 云函数 `getNearbyPoi` 本身方向中立（仅"抽象词 → POI 品类"转译），**零改动**，方向性由前端标签 + 用户输入词驱动。

### 4.3 `getStats`（达标判定算法）

`judgeNutr` / `summarizeWindow` / `buildWeeks` 统一口径：

```
gain 达标：Calorie >= Target && Protein >= Target
lose 达标：Calorie <= Target && Protein >= Target
```

- 核心语义：**蛋白两种模式都须达标**；**热量在 gain 是"地板"（要吃够），在 lose 是"天花板"（不能超）**。
- 无目标（target ≤ 0）时 `nutr = 'none'`，不计入达标率。

---

## 五、评审 CheckList 与测试回归矩阵

| # | 检查点 | 验收标准 | 当前状态 |
|---|---|---|---|
| 1 | 老用户无缝兼容 | `users.goal_type` 缺失时全链路按 gain，无报错、无修库 | ✅ 已落地 |
| 2 | 归一化收敛 | 所有 goal_type 判断均经 `normalizeGoalType`，无散落 `=== 'lose'` | ✅ 已落地 |
| 3 | 食谱独立性 | gain Prompt/常量 100% 原样；lose 独立常量 + 独立 fallback | ✅ 已落地 |
| 4 | 问候语隔离 | 五时段 × 双模式文案分离，无增重向泄漏 | ⚠️ 待落地 |
| 5 | 减重双环保底/超标文案 | 显示「还可吃 N / 已超标 N kcal」差值 | ⚠️ 待落地 |
| 6 | 超标高亮规范 | lose 超标热量环警示红 `#FF4D4F` | ⚠️ 现为 `#F44336`，待统一 |
| 7 | 达标判定正确性 | getStats 双方向 `judgeNutr` 口径正确 | ✅ 已落地 |
| 8 | 活动系数档位 | 是否补 very_active 1.95 档 | ❓ 评审决策 |
| 9 | 减重热量缺口 | `TDEE−350` vs `TDEE−500` 统一 | ❓ 评审决策 |
| 10 | 回归测试 | `npx jest` 全绿；真机双模式各跑一遍 UI + 云函数 | ✅ 586 通过（真机待验证） |

---

## 附：评审待决事项（Decision Log）

1. **减重热量缺口**：350 → 500？（营养处方差异，需产品/营养师确认）
2. **活动系数 very_active 档**：是否补 1.95？
3. **警示红统一**：`#F44336` → `#FF4D4F`？
4. **问候语 & 双环差值文案**：列入 Phase 3 落地清单。