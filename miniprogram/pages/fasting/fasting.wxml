<view class="fasting-page">
  <view class="fasting-card">
    <view class="status-label">{{isEating ? '进食窗口 · 可进食' : '断食中 · 坚持住'}}</view>
    <view class="remain-num">{{isEating ? '距窗口结束' : '距下次进食'}}</view>
    <view class="remain-time">{{remainText}}</view>

    <view class="progress-bar">
      <view class="progress-fill" style="width: {{progressPct}}%;"></view>
    </view>
    <view class="progress-sub">断食进度 {{progressPct}}% · 已断食 {{elapsedText}}</view>

    <view class="phase-box">
      <text class="phase-title">{{phase.title}}</text>
      <text class="phase-desc">{{phase.desc}}</text>
    </view>
  </view>

  <view class="offset-card">
    <text class="offset-label">进食窗口偏移（分钟）</text>
    <input class="offset-input" type="number" value="{{offsetMin}}" bindchange="onOffsetChange" />
    <text class="offset-hint">默认 12:00–20:00（8 小时进食 / 16 小时断食）</text>
  </view>

  <view class="stage-list">
    <view class="stage-item"><text class="dot dot-1"></text>0-8h 消化期</view>
    <view class="stage-item"><text class="dot dot-2"></text>8-12h 动用脂肪</view>
    <view class="stage-item"><text class="dot dot-3"></text>12-14h 深度燃脂</view>
    <view class="stage-item"><text class="dot dot-4"></text>14-16h 细胞自噬</view>
  </view>

  <!-- 新增：轻断食用法与规则说明卡片 -->
  <view class="guide-card">
    <view class="guide-title">
      <text class="title-icon">💡</text>
      <text class="title-text">16:8 轻断食怎么用？</text>
    </view>

    <view class="guide-intro">
      <text class="intro-icon">🔥</text>
      <text class="intro-text">断食的目的：拉长空腹时间，让身体在断食期从「消耗糖原」切换到「燃脂供能」，同时给肠胃留出休息时间，配合热量缺口更易掉秤。</text>
    </view>

    <view class="guide-section">
      <view class="section-item">
        <text class="item-tag tag-eat">8h 进食期</text>
        <text class="item-desc">在设定的 8 小时内（默认 12:00–20:00）正常吃三餐或两餐，吃饱即可，避免暴饮暴食。</text>
      </view>

      <view class="section-item">
        <text class="item-tag tag-fast">16h 断食期</text>
        <text class="item-desc">其余 16 小时内不摄入有热量的食物。可正常饮水、无糖黑咖啡、无糖茶。</text>
      </view>
    </view>

    <view class="guide-tip">
      <text class="tip-icon">✏️</text>
      <text class="tip-text">作息不同？修改上方的「偏移分钟」，如输入 -120 即可将进食窗口调为 10:00–18:00。</text>
    </view>
  </view>
</view>