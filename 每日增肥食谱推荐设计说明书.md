# 《"每日增肥食谱推荐"模块详细设计说明书》

> 项目：BeFat 增肥记录小程序
> 分支：`每日增肥食谱推荐`
> 基线：`master @ 9139859`
> 涉及：新增云函数 `getDailyMenu`、`toggleFavoriteRecipe`，新增页面 `pages/daily-menu/`，扩展 `daily_menus`/`user_favorites` 集合
> 状态：设计定稿

---

## 1. 模块架构与时序图

### 1.1 设计目标

- **懒加载生成**：不做预生成、不跑定时任务；当日第一位访问用户触发生成，后续用户直接读库。
- **每日不重样**：以日期 `YYYY-MM-DD` 为主键，一天只生成一次，天然保证"每日换菜单"。
- **零人工运维**：AI 生成 → 自动落库 → 自动兜底，无后台管理界面。

### 1.2 getDailyMenu 完整处理时序

```
客户端 A/B/C（当日首秒并发）          getDailyMenu 云函数                     GLM-4-Flash
─────────────────────               ────────────────────                    ───────────
callFunction({ date })  ──▶
                                    ① 查 daily_menus._id = date
                                        ├─ 命中 status=READY ──────────▶ 直接返回缓存（AC-1）
                                        ├─ 命中 status=GENERATING ─────▶ 轮询等待（见 1.3）
                                        └─ 未命中 ──▶ ② add 占位文档
                                                    { _id: date, status:'GENERATING', created_at:now }
                                        │
                     ┌── add 成功（唯一 winner，如 A）──▶ ③ 调 GLM-4-Flash 生成 4 餐
                     │                                  ┌───────────── 生成中 ─────────────┐
                     │                                  │  system/用户 prompt（含今日日期）   │
                     │                                  └────────────▶ 返回 Markdown/JSON ◀───┘
                     │                                  ④ stripCodeFence 清理 Markdown 标记
                     │                                  ⑤ JSON.parse → 结构 + 类型校验
                     │                                  ⑥ JS reduce 重新求和 total_calorie/total_protein_g
                     │                                  ⑦ update 占位文档为 status:'READY' + meals + 汇总
                     │                                  ⑧ 返回结果
                     └── add 失败（主键冲突 = B/C）──▶ ⑨ sleep 后回到 ① 重新查库（轮询）
                                                          → 读到位后返回（读 A 写好的 READY 数据）
```

### 1.3 高并发防刷策略（防重复唤醒 AI）

微信云数据库 `_id` 天然唯一，利用它实现**乐观锁占位**：

| 步骤 | 动作 | 结果 |
|------|------|------|
| 1 | 所有未命中请求 `add` 占位文档 `{_id: date, status:'GENERATING', created_at: serverDate}` | **仅一个请求成功**（唯一 winner），其余抛主键冲突 |
| 2 | winner 调 AI 生成，完成后 `update` 为 `status:'READY'` + 数据 | 落库 |
| 3 | 冲突方（loser）捕获冲突 → `sleep(800ms)` 后重新查库 | 读 winner 写好的数据返回 |
| 4 | **僵尸保护**：若读到 `GENERATING` 且 `now - created_at > 120s`，判定 winner 已崩溃 → 删除僵尸占位 → 重新走 add 占位 | 自愈 |

> 说明：不引入第三方 Redis/分布式锁，纯靠 `_id` 主键唯一性 + 时间戳僵尸判定，符合微信云开发零依赖约束。轮询上限设 **5 次 × 800ms（总等待约 4s）**，覆盖 GLM-4-Flash 正常 1.5s~3s 的生成耗时，避免 Loser 在 Winner 仍等模型返回时提前触发 `code 93`；超限才返回兜底。

---

## 2. 数据库 Schema 详细定义

### 表 1：`daily_menus`（主键 `_id` = 日期）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `_id` | string | 是 | 主键，格式 `YYYY-MM-DD`（**北京时间 Asia/Shanghai**，服务端显式 +8 小时偏移格式化，见 4.1 `fmtBeijingDate`） |
| `date` | string | 是 | `YYYY-MM-DD`，冗余便于查询 |
| `status` | string | 是 | `'READY'` \| `'GENERATING'`（占位态） |
| `meals` | array | 是 | 4 餐数组，每项见下方子结构 |
| `total_calorie` | number | 是 | **后端 JS 求和**，非模型输出 |
| `total_protein_g` | number | 是 | **后端 JS 求和**，非模型输出 |
| `generated_by` | string | 是 | 模型名，如 `glm-4-flash` |
| `generated_at` | date | 否 | AI 生成完成时间 |
| `created_at` | date | 是 | 占位/首次创建时间（僵尸判定用） |
| `updated_at` | date | 是 | 最后更新时间 |

`meals` 子结构：

```jsonc
{
  "meal_type": "breakfast",   // breakfast/lunch/snack/dinner（沿用现有枚举）
  "title": "花生酱香蕉全麦吐司",
  "calorie": 480,             // 模型给出，后端会参与求和但以输入为准做类型校验
  "protein_g": 18.5,
  "ingredients": ["全麦吐司 2片", "花生酱 1勺", "香蕉 1根"],
  "steps": ["吐司烤至微黄", "抹花生酱", "摆香蕉片"]
}
```

### 表 2：`user_favorites`（扩展快照收藏）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `_openid` | string | 是 | 微信云开发自动注入，收藏归属 |
| `recipe_id` | string | 否 | 现有普通食谱收藏用（指向 `recipes._id`），每日菜单快照收藏时为 `null` |
| `recipe_title` | string | 是 | 菜名（每日菜单快照收藏的去重键之一） |
| `meal_type` | string | 否 | 餐次类型（`breakfast/lunch/snack/dinner`，每日菜单快照收藏用；普通食谱收藏为 `null`），参与唯一索引 |
| `recipe_snapshot` | object | 是 | 单菜品快照：`{title, calorie, protein_g, meal_type, ingredients, steps, date}` |
| `created_at` | date | 是 | 收藏时间 |

**联合唯一索引**：`idx_openid_meal_title`（`_openid` + `recipe_title` + `meal_type`），防止不同日期生成同名泛化菜（如"水煮蛋"）时，用户跨日收藏互相误取消。

> ⚠️ 兼容性说明：现有 `user_favorites` 以 `recipe_id` 去重(`toggleFavorite`/`getFavorites`)，本次新增 `recipe_title`/`meal_type`/`recipe_snapshot` 三个字段用于每日菜单快照收藏，两种收藏共存于同一集合。旧函数读 `recipe_id`、新函数 `toggleFavoriteRecipe` 读 `recipe_title`+`meal_type`，互不干扰。需在控制台为 `user_favorites` 补建 `idx_openid_meal_title` 唯一索引（若索引创建失败，代码内先查后写做逻辑兜底）。

---

## 3. GLM-4-Flash 提示词工程

### 3.1 系统 Prompt

```
你是一位专业的中国增重营养师。你的任务是为「吃不胖、想增重」的用户设计一日三餐。你只输出严格 JSON，不输出任何解释、标题、Markdown 代码块或多余文字。
```

### 3.2 用户 Prompt（传入当日日期 date）

```
今天是 {DATE}。请为增重人群设计一日增肥食谱，包含 4 餐：早餐(breakfast)、午餐(lunch)、加餐(snack)、晚餐(dinner)。

每一餐必须包含以下字段：
- title: 菜品名称（中文，吸引人）
- calorie: 该餐热量估算值(kcal，整数)
- protein_g: 该餐蛋白质估算值(g，可含 1 位小数)
- ingredients: 食材数组，每项格式"食材名 + 份量"，如"全麦吐司 2片"
- steps: 烹饪步骤字符串数组，2~4 步，简洁

严格输出以下 JSON 结构，不要计算 total_calorie 或 total_protein_g：

{"meals":[{"meal_type":"breakfast","title":"...","calorie":0,"protein_g":0,"ingredients":["..."],"steps":["..."]},{"meal_type":"lunch",...},{"meal_type":"snack",...},{"meal_type":"dinner",...}]}

约束：
1. 4 餐必须齐全，meal_type 依次为 breakfast/lunch/snack/dinner
2. 每餐 calorie 在 150~900 之间（加餐 snack 可放宽至 80~900），protein_g 在 0~80 之间
3. 高热量、高蛋白，偏增重导向，但食材常见、可落地
4. 菜名避免过于泛化（如"水煮蛋""牛奶"），需加有辨识度的修饰（如"溏心水煮蛋配黑胡椒""香蕉燕麦奶昔"）
5. 不要输出任何数学总和，总和由后端计算
只返回 JSON 本体。
```

### 3.3 生成参数

| 参数 | 值 | 依据 |
|------|-----|------|
| `model` | `glm-4-flash`（经 `MENU_MODEL` 环境变量可切） | 免费文本模型，成本低 |
| `temperature` | `0.25`（范围 0.2~0.3） | 保留少量创意变化，避免过高导致 JSON 崩坏 |
| `max_tokens` | `1500` | 4 餐完整 JSON 需约 1000~1400 tokens |
| `timeout` | `15000` | 文本生成 15s 兜底 |

---

## 4. 核心云函数接口契约与伪代码

### 4.1 getDailyMenu

**入参**：`{ date?: 'YYYY-MM-DD' }`（缺省用北京时间当天）

**出参（成功）**：

```jsonc
{
  "code": 0,
  "message": "ok",
  "data": {
    "date": "2026-08-21",
    "meals": [ { "meal_type":"breakfast", "title":"...", "calorie":480, "protein_g":18.5, "ingredients":[...], "steps":[...] } ],
    "total_calorie": 1950,
    "total_protein_g": 96.3,
    "generated_by": "glm-4-flash"
  }
}
```

**核心伪代码**：

```js
const FALLBACK_MENUS = [ /* 预置 4 餐兜底数据，静态常量 */ ]

// 云函数 Node.js 默认 UTC+0，须显式 +8 小时，避免每天 00:00~08:00（北京时间）生成"昨天"日期
function fmtBeijingDate(d = new Date()) {
  const bj = new Date(d.getTime() + 8 * 3600 * 1000)
  return bj.toISOString().slice(0, 10)   // => 'YYYY-MM-DD'（Asia/Shanghai）
}

async function fetchOrGenerate(date) {
  // ① 查缓存
  let doc = await db.collection('daily_menus').doc(date).get().then(r => r.data)
  if (doc && doc.status === 'READY') return toDto(doc)

  // ② 未命中：占位锁
  if (!doc) {
    try {
      await db.collection('daily_menus').add({ data: { _id: date, date, status:'GENERATING', created_at: db.serverDate() } })
      // 我是 winner → 走生成
    } catch (e) {
      // 主键冲突 → 并发 loser → 轮询
      return await waitAndRead(date)   // 5 次 × 800ms（约 4s），超时 → code 93
    }
  } else if (doc.status === 'GENERATING') {
    if (Date.now() - doc.created_at > 120000) {
      // 僵尸占位，删除后重试
      await db.collection('daily_menus').doc(date).remove()
      return await fetchOrGenerate(date)
    }
    return await waitAndRead(date)
  }

  // ③ 调 GLM-4-Flash 生成
  const raw = await callGlmMenu(date)             // model/temp/max_tokens 见第 3 节
  const meals = parseAndValidate(raw)             // 见下
  // ④ 后端强制重算总和
  const total_calorie = meals.reduce((s, m) => s + m.calorie, 0)
  const total_protein_g = Math.round(meals.reduce((s, m) => s + m.protein_g, 0) * 10) / 10

  // ⑤ 落库
  await db.collection('daily_menus').doc(date).update({ data: {
    status:'READY', meals, total_calorie, total_protein_g,
    generated_by: model, generated_at: db.serverDate(), updated_at: db.serverDate()
  }})
  return { date, meals, total_calorie, total_protein_g, generated_by }
}

function parseAndValidate(raw) {
  const clean = stripCodeFence(raw)               // 清 ```json / ```
  const obj = JSON.parse(clean)                    // 抛错 → 外层 catch → code 93
  const meals = obj.meals
  if (!Array.isArray(meals) || meals.length !== 4) throw new Error('meals 须为 4 餐')
  const order = ['breakfast','lunch','snack','dinner']
  return order.map((type, i) => {
    const m = meals.find(x => x.meal_type === type) || meals[i]
    const calorie = Math.round(Number(m.calorie) || 0)
    const protein = Math.round((Number(m.protein_g) || 0) * 10) / 10
    const calMin = type === 'snack' ? 80 : 150     // 加餐热量下限单独放宽，避免误杀"一杯牛奶"类合理加餐
    if (!(calorie >= calMin && calorie <= 900)) throw new Error('calorie 越界')
    if (!(protein >= 0 && protein <= 80)) throw new Error('protein 越界')
    return { meal_type: type, title: m.title || '推荐餐', calorie, protein_g: protein,
             ingredients: Array.isArray(m.ingredients) ? m.ingredients : [],
             steps: Array.isArray(m.steps) ? m.steps : [] }
  })
}
```

**异常兜底**：GLM 超时/返回非 JSON/结构校验失败 → 捕获后返回 **`code 93`** + 预置兜底食谱（`FALLBACK_MENUS`），前端以同一结构渲染，唯一差异是 `data.from_fallback: true`，可提示"今日智能食谱暂不可用，已为你准备备用食谱"。

| code | 含义 | 触发 | 前端动作 |
|------|------|------|----------|
| `0` | 成功 | — | 渲染 |
| `1` | 缺参数/日期非法 | `date` 格式错误 | toast 提示 |
| `93` | 生成失败已兜底 | GLM 超时/JSON 崩坏/并发超时 | 渲染备用食谱 + 轻提示 |

### 4.2 toggleFavoriteRecipe

**入参**：`{ recipe_snapshot }`（前端把当前单餐完整快照传入）

**出参**：`{ code:0, data:{ favorited: true|false } }`

**核心伪代码（基于 `_openid` + `recipe_title` + `meal_type` 原子切换）**：

```js
exports.main = async (event) => {
  const openid = cloud.getWXContext().OPENID
  const snap = event.recipe_snapshot
  if (!snap || !snap.title) return { code: 1, message: '缺少 recipe_snapshot' }

  const existing = await db.collection('user_favorites').where({
    _openid: openid, recipe_title: snap.title, meal_type: snap.meal_type
  }).get()

  let favorited
  if (existing.data.length > 0) {
    await db.collection('user_favorites').doc(existing.data[0]._id).remove()
    favorited = false
  } else {
    await db.collection('user_favorites').add({
      data: {
        _openid: openid,
        recipe_id: null,                 // 快照收藏无 recipes 主键
        recipe_title: snap.title,
        meal_type: snap.meal_type,       // 参与唯一索引，避免跨日同名泛化菜互相覆盖
        recipe_snapshot: snap,           // { title, calorie, protein_g, meal_type, ingredients, steps, date }
        created_at: db.serverDate()
      }
    })
    favorited = true
  }
  return { code: 0, message: 'ok', data: { favorited } }
}
```

> 唯一性由 `idx_openid_meal_title` 唯一索引保证；索引未建成时靠 `where(_openid, recipe_title, meal_type)` 先查后写的逻辑去重兜底。

---

## 5. 质量保障与测试用例矩阵

### AC-1：缓存命中 / 未命中性能

| 用例 | 前置 | 步骤 | 预期 |
|------|------|------|------|
| 缓存命中 | `daily_menus` 已有当天 `READY` 文档 | 调 getDailyMenu | 返回落库数据，**不触发 axios**，`duration < 100ms` |
| 首次生成 | 无当天文档，mock axios 返回合法 4 餐 JSON | 调 getDailyMenu | 生成 + 落库，`axios` 被调用 1 次，返回 `code 0` |
| 并发防重 | 无文档，3 个并发调用，mock add 使仅 1 个成功 | `Promise.all` 3 个请求 | **axios 仅被调用 1 次**，3 个请求最终都返回 `code 0` |
| 并发轮询覆盖 | Winner 生成耗时 2.5s（模拟 GLM 正常耗时），Loser 进入轮询 | mock 让 Loser 5×800ms 内读到 `READY` | Loser 返回 `code 0`，**不提前触发 code 93** |
| 僵尸占位恢复 | 文档 `GENERATING` 且 `created_at` 早于 120s | 调 getDailyMenu | 删除僵尸 → 重新生成 → `code 0` |

### AC-2：后端数值强制校验重算

| 用例 | 步骤 | 预期 |
|------|------|------|
| 总和重算 | mock 模型返回 meals 但**不返回** total（或故意给错 total） | 后端 `reduce` 求和，`total_calorie`/`total_protein_g` 与实际累加一致，**不信任模型总和** |
| 蛋白精度 | meal protein_g 为 12.34 / 5.6 | `total_protein_g` 四舍五入到 1 位小数 |
| 类型脏数据 | meal calorie 传字符串 `"480"` | `Number()` 归一为 480 |
| 越界拦截 | breakfast calorie=95（低于 150）或任意餐 calorie=2500 / 缺 meal_type | 抛错 → `code 93` 兜底 |
| 加餐下限放宽 | snack calorie=120（原 150 下限会误杀"一杯牛奶"类合理加餐） | 通过校验（`snack` 下限 80），**不误杀** |
| 缺餐拦截 | 仅返回 3 餐 | 抛错 → `code 93` 兜底 |

### AC-4：时区与收藏去重（补强项）

| 用例 | 步骤 | 预期 |
|------|------|------|
| 凌晨日期正确 | mock 系统时间为北京时间 00:30（UTC 前一日 16:30） | `fmtBeijingDate` 返回**当天**日期，无"昨天"越界 |
| 跨日同名不误消 | 用户收藏"水煮蛋"(breakfast) 与 "水煮蛋"(snack) 两条快照 | 两条独立存在，取消其一不影响另一（`meal_type` 参与去重） |
| 跨日同名菜 | 不同日期 AI 生成同名"水煮蛋"快照 | 因 Prompt 要求修饰词 + `meal_type` 兜底，不串号 |

### AC-3：JSON 破坏 / API 超时兜底

| 用例 | 步骤 | 预期 |
|------|------|------|
| Markdown 包裹 | 模型返回 ` ```json\n{...}\n``` ` | `stripCodeFence` 剥壳后正常解析 |
| JSON 崩坏 | 模型返回 `"抱歉，我无法..."` 纯文本 | `JSON.parse` 抛错 → `code 93` 返回预置食谱 |
| API 超时 | mock axios reject `ECONNABORTED` | 捕获 → `code 93`，前端不白屏 |
| 空响应 | 模型返回空 content | 抛错 → `code 93` |
| 兜底结构完整性 | 触发 93 | 返回的预置食谱仍含 4 餐 + 正确总和，前端可正常渲染 |

---

## 6. 前端 UI 与组件规范

### 6.1 Design Tokens（继承现有，零新增）

| 用途 | Token | 值 |
|------|-------|-----|
| 主色 | `var(--primary)` | `#E8471F` |
| 主色背景 | `var(--primary-bg)` | `#FFF8E7` |
| 强调色 | `--color-accent-yellow` / `--color-accent-green` | `#FFD23F` / `#639922` |
| 卡片 | `.card`（白底 / `--radius-md` 24rpx / 4rpx 粗描边 / `--shadow-card`） | 沿用 |
| 圆角 | `--radius-sm/md/lg` | 16/24/40 rpx |
| 文字 | `--text-primary/secondary/light` | `#1A1006` / `#666` / `#999` |

### 6.2 页面结构（新增 `pages/daily-menu/`）

```
┌──────────────────────────────┐
│  顶部汇总卡（Pill 组件）       │  「今日总热量 1950 kcal · 蛋白质 96g」
│  [早餐 pill] [午餐 pill] [加餐 pill] [晚餐 pill]   ← 横向 Pill tab 锚点
├──────────────────────────────┤
│  单列四餐卡片（垂直滚动）       │
│  ┌ 早餐 · 花生酱香蕉吐司   ♡  ┐
│  │  480 kcal · 18.5g 蛋白     │
│  │  食材/步骤（默认收起）      │
│  └────────────────────────────┘
│  ┌ 午餐 · ...             ♡  ┐
│  ...（每餐一张卡片）          │
└──────────────────────────────┘
```

### 6.3 组件规范

- **顶部汇总**：标准胶囊 Pill 组件——`display:inline-flex; background:var(--primary-bg); border:4rpx solid var(--color-border); border-radius:999rpx; padding:12rpx 28rpx`，字号 `28rpx`，主色数字加粗。
- **四餐列表**：单列卡片结构，复用 `.card`（`margin:16rpx 30rpx; padding:30rpx; radius 24rpx; 4rpx 粗描边`），餐次名用主色小标签，菜名 `32rpx` 加粗，营养值 `--text-secondary`。
- **收藏按钮**：爱心 Icon 空心/实心切换 —— 空心 `♡`（`--text-light`）/ 实心 `♥`（`--primary`），`bindtap` 绑定 `toggleFav`，调用 `toggleFavoriteRecipe`；**乐观更新**（先翻转本地状态，失败回滚 + toast）。
- **食材/步骤**：默认收起，点按展开（`wx:if` 控制），避免四餐信息过载。

### 6.4 骨架屏（AI 生成中）

未命中缓存（占位 `GENERATING` 或首次生成中）时展示 Skeleton Screen 占位，避免白屏：

```xml
<view class="card skeleton" wx:if="{{loading}}">
  <view class="sk-line sk-pill"></view>          <!-- 汇总 Pill 占位 -->
  <view class="sk-line" style="width:70%"></view> <!-- 菜名占位 -->
  <view class="sk-line" style="width:40%"></view> <!-- 营养值占位 -->
</view>
```

```css
.skeleton { opacity: 0.6; }
.sk-line { height: 32rpx; border-radius: 8rpx;
  background: linear-gradient(90deg, #F0E7DC 25%, #FAF3E9 50%, #F0E7DC 75%);
  background-size: 400% 100%; animation: slide 1.2s linear infinite; }
@keyframes slide { from { background-position: 100% 0; } to { background-position: 0 0; } }
```

并叠加"智能食谱生成中，请稍候…"轻提示；若返回 `code 93`，替换为"已为你准备备用食谱，试试看今天的搭配"。